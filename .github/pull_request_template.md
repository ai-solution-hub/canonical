## Summary

<!-- What this PR does and why. -->

## Change-log entries

<!--
  id-390 structured change-log (Warp capture model, surface-keyed prefixes).
  Fill in one line per change worth recording — an EVENT, not a state
  restatement ("added X to Y", not "the schema now has Y"). Delete unused
  prefix lines or leave them blank; blank lines are never harvested.

  Surfaces —
    SCHEMA:   database schema change (migration adds also auto-harvest)
    DEPLOY:   deploy-topology / infrastructure change
    PRODUCT:  user-facing behaviour change
    WORKFLOW: dev-tooling / skills / agents / process change
    FIX:      bug or regression fixed

  Entries are harvested into the docs-site change-log (reference/change-log/)
  at aggregation time; the same grammar also works as a main commit-message
  trailer. Reference: docs-site AGENTS.md §6.
-->

CHANGELOG-SCHEMA: CHANGELOG-DEPLOY: CHANGELOG-PRODUCT: CHANGELOG-WORKFLOW: CHANGELOG-FIX:
