# Knowledge Hub Pipeline Dependencies
# Core AI
anthropic==0.79.0
openai==2.21.0
pydantic==2.12.5

# Morphology (tag singularisation — parity with TS pluralize via inflect.singular_noun)
inflect==7.5.0

# Content extraction
trafilatura==2.0.0
requests==2.33.0
extruct==0.18.0
# beautifulsoup4 pin consolidated to the single ==4.15.0 declaration below (docling's
# HTML backend section) — previously duplicated here as a floating >=4.12.0 floor,
# which is a strict subset of ==4.15.0 anyway ({127.22}, S438 lockfile follow-up).
lxml>=5.0.0

# PDF extraction (used by Python scripts only; JS routes use unpdf)
pdfplumber==0.11.9

# XLSX extraction (used by ID-52 Path B Python form extractor;
# transitive via cocoindex today but pinned explicitly as a direct
# dependency because scripts/cocoindex_pipeline/form_extractors/xlsx.py
# treats it as a first-class production dependency).
openpyxl==3.1.5

# Document processing (used by Python scripts only; JS routes use mammoth + Claude)
python-docx>=1.1.2
docxtpl>=0.19.0
# pandoc (Track-Changes resolution in docx_utils.py) ships as a pip-bundled
# static binary: the cocoindex image is buildpack-built ({66.7}) with no apt
# layer, so a system pandoc is unavailable — pypandoc_binary bundles one that
# docx_utils._resolve_pandoc_path() finds via pypandoc.get_pandoc_path() ({80.3}).
pypandoc_binary>=1.13

# Environment
# Floor pinned to CocoIndex's own requirement (>=1.1.0): litellm's loose
# `python-dotenv>=0.2.0` would otherwise let a resolver pick a version below
# CocoIndex's floor and break the Stage-4 LiteLLMEmbedder import path (ID-49.2).
python-dotenv>=1.1.0

# Supabase client (bid worker)
supabase>=2.0.0

# Evaluation metrics
bert-score>=0.3.13

# Cocoindex pipeline (ID-28 / T8 cocoindex sidecar)
# cocoindex requires dangerouslyDisableSandbox in Claude Code dev (CLAUDE.md gotcha).
# Pin 1.0.7 (bl-252): pulls 1.0.4 SQL-injection hardening + NUL-stripping in text/jsonb
# ingest; no breaking changes to any USED option across 1.0.4-1.0.7 and the memo-fingerprint
# scheme is unchanged (ID-89 RESEARCH §3.3, changelog-verified; bl-239 probe
# scripts/tests/test_file_branch_memo_fingerprint.py stays the executable guard). Bump must
# precede the ID-45 full-corpus re-ingest so the run lands on 1.0.7.
cocoindex[postgres]==1.0.7
# Docling: MIT-licensed document converter + layout model (1.8 GB weights).
# Pre-warmed into the image at build time so the model weights ship in-image.
# Pinned to docling-slim with NO-OCR extras (S437): the full `docling` meta-package
# pulls docling-slim[standard], which force-installs rapidocr → opencv-python (FULL,
# X11-linked). In the buildpacks image (no X11/apt layer) the always-on layout model
# (docling-ibm-models) imports cv2 → `libxcb.so.1: cannot open shared object file` on
# EVERY PDF convert (not just OCR; do_ocr=False already set in adapters.py). The extras
# below give PDF/DOCX/XLSX + chunking WITHOUT rapidocr; opencv-python-headless (X11-free)
# supplies cv2 for the layout model. Pinned to stop unpinned-rebuild drift re-pulling
# the full opencv variant.
docling-slim[format-pdf-docling,models-local,format-docx,format-xlsx,feat-chunking]==2.108.0
opencv-python-headless==4.13.0.92
# docling 2.108 eager-backend + TableFormer deps the no-OCR extra set omits (S437):
# document_converter.py EAGERLY imports ALL format backends at module load
# (pptx → latex → markdown → html), and the PDF TableFormer model needs scipy — so a
# PDF/DOCX/XLSX convert ImportErrors without these even though the extras above don't
# pull them. Verified in-container (both synthetic PDFs convert, layout+TableFormer on
# headless cv2, no libxcb). None depend on opencv → headless guarantee preserved.
# (beautifulsoup4 = docling's HTML backend; also the sole pin for the general content
# extraction use above — consolidated from a duplicate >=4.12.0 declaration, S438.)
python-pptx==1.0.2
pylatexenc==2.10
marko==2.2.3
beautifulsoup4==4.15.0
scipy==1.18.0
# faiss-cpu: similarity index backing Stage-5 entity-resolution canonicalisation
# (TECH §P-12 / Inv-18). Exact-pinned to 1.14.2 for deterministic IndexFlatIP behaviour.
faiss-cpu==1.14.2
# litellm: backs the Stage-4 embedding op — cocoindex.ops.litellm.LiteLLMEmbedder
# ("text-embedding-3-large", dimensions=1024) → vector(1024) (ID-49.2). NOT used
# for LLM extraction (that path is Anthropic-direct via a custom @coco.fn — there
# is no ExtractByLlm in cocoindex (absent through 1.0.7), per the S256 Path-A amendments).
litellm>=1.40.0,<2.0.0
# aiohttp: HTTP framework for cocoindex sidecar wrapper server (ID-28.15). Transitive via
# litellm, pinned explicitly because scripts/cocoindex_pipeline/server.py treats it as a
# first-class production dependency (Cloud Run sidecar HTTP server).
aiohttp>=3.9.0,<4.0.0
# asyncpg: async Postgres driver for cocoindex postgres source/sink connector.
asyncpg>=0.30.0
httpx==0.28.1  # explicit pin — adapters.py uses httpx.AsyncClient; transitive via cocoindex today (fragile)
# tenacity: KH-managed retry wrapper around anthropic.AsyncAnthropic.messages.create
# (ID-28.17). cocoindex 1.0.7 has no retry primitives around the @coco.fn LLM path
# (empirically verified in S257 W2a + the _FlowRetryCounter docstring; unchanged
# through 1.0.7 per ID-89 RESEARCH §3.3); KH owns the
# retry surface, decorated via tenacity's @retry with 3 attempts + exponential backoff
# (P-OQ2: 1 s base / 2x exponent / 30 s cap).
tenacity==9.1.4
# tiktoken: backs the bl-223 embedding-input truncation guard
# (flow.py::_truncate_embedding_input) — `text-embedding-3-large` has a HARD
# 8192-token OpenAI input ceiling, so the document/chunk text is bounded to
# `cl100k_base` token budget before `.embed()` or a 400 aborts the whole ingest.
# Transitive via litellm (which declares `tiktoken>=0.7.0` and uses the same
# encoding for token accounting); pinned explicitly as a first-class dependency
# because the guard treats it as one (mirrors the aiohttp/asyncpg/httpx pins).
tiktoken>=0.7.0
# sqlglot: SQL parser backing tools/ast_dataflow_py column-reads/column-writes
# (cross-language column lineage — Python-side raw-SQL statements over the same
# tables the TS ast-dataflow tool covers). OPTIONAL at runtime: the tool
# degrades to a word-boundary regex fallback (confidence 'indirect') when
# sqlglot is not importable; installing it upgrades SQL hits to 'exact'.
sqlglot>=26.0.0

# Testing
pytest>=8.0.0
