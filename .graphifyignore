# .graphifyignore — Knowledge Hub
# Authored 2026-05-08 (Phase 0.8 dispatch)
# Per-run overrides possible by passing target directory explicitly.

# Build / framework artefacts
node_modules/
.next/
dist/
build/
coverage/
playwright-report/
test-results/
*.tsbuildinfo

# Worktree clones (holds full repo copies)
.worktrees/

# Other framework artefacts
.firecrawl/
.vercel/
.pytest_cache/

# Graphify output / scratch / cache
graphify-out/
.tmp/
.cache/
*.snap

# Sensitive client + private content
# (run graphify against these explicitly when intentional, e.g. for client-docs eval)
data/
client-documentation/
docs/continuation-prompts/
docs/generated/
e2e/.auth/

# Local config (runtime / staging-specific)
supabase/.temp/
.env
.env.local
.env*.local
.env*.old-project-backup
.env*-backup
.env.bak
.mcp.json

# Auto-generated (re-derivable; not architecturally meaningful)
supabase/types/database.types.ts

# Note: .planning/.archive/ INCLUDED for full-codebase semantic runs
# Decision 2026-05-08 — historical specs/research valuable for graph;
# same privacy posture as MCP exposing client docs to Claude clients.

# Note: __tests__/ INCLUDED — tests are part of the codebase signal.
