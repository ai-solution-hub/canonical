{
  "$schema-note": "Counts-only baseline for scripts/check-knip-baseline.ts. Each key maps to the per-file array name in knip --reporter json. CI fails if any current count exceeds the matching baseline. Update procedure: docs/runbooks/ci.md §6.",
  "capturedAt": "2026-06-07",
  "capturedFromCommit": "S320 ID-85.4 (bl-235) — types 73→71 (-2): lock in the post-{52.23} reduction per ci.md §6.3 (reductions are re-baselined downward, never left slack); all other counts unchanged (verified with an unsandboxed `bun run knip`: dependencies 3, unlisted 3, exports 105, types 71). Previously: S267 parallel-cmux fleet integration (ID-9/34/35/50/51) — exports 101→105 (+4): the new lib/ledger/ public API surface authored by the ledger-cli sub-orchestrator (ID-34/35) — applyTaskListPatches/applyRoadmapPatches/applyBacklogPatches (lib/ledger/patch-apply.ts), KNOWN_DOCUMENT_NAMES (lib/ledger/detect-schema.ts), and SubtaskStatus (lib/validation/task-list-schema.ts). They are consumed by scripts/ledger-cli.ts + the ledger-cli test suites, but knip's project config treats scripts/tests as non-consumers, so they read as unused exports — the same established pattern as the ast-dataflow public surface + R-WP17 schemas below. unlisted held at 3 (the +7 yaml hits from the new docs-site test files were FIXED, not baselined: yaml added to package.json devDependencies). Previously: S265-ast ID-32.28 (defect-B5 binding fix) — exports 94→101 (+7): +6 are the hand-authored defect-B5 ResponseSchemas in lib/validation/schemas.ts (EntityCoOccurrenceResponseSchema, CoverageTargetsPutResponseSchema, BatchReviewResponseSchema, BatchWorkspacesResponseSchema, ItemPatchResponseSchema, ItemDeleteResponseSchema) — they CORRECT the wrong 32.20 Source-A bindings and are bound at codemod-inference time via the route+method override in scripts/codemods/inference-source-a.ts; the working-tree routes are intentionally NOT wrapped here (OQ-10 re-scope — the corpus rollout that consumes them at app runtime is Task ID-50), so like the 37 R-WP17 schemas they read as unused exports until ID-50 lands. The remaining +1 (95th export) is a PRE-EXISTING drift already present at worker-branch HEAD 659469c2 (NOT introduced by ID-32.28) — flagged to the orchestrator for separate triage. Previously: S263 (id32 integration, 2nd consolidation) — exports 60→94 (+34): 37 R-WP17 response-schema constants authored in lib/validation/schemas.ts by ID-32.20 (OPS-T1 codemod); they are intentional API-response schemas awaiting --apply route wiring (deferred to a post-merge ID-32 subtask), so currently unconsumed by app runtime and flagged by knip. types 78→73 (-5): backlog-schema BacklogStatus re-export dedup + lib/ast-dataflow excluded from build tsconfig. Previously: a2c5ccf8 (chore/outstanding-cleanup-investigation, post-prod-readiness merge S57) — types 34→78 (+44): 34 are deliberate ast-dataflow public-API surface re-exported from lib/ast-dataflow/index.ts per HANDOVER.md §10 (BaseResult, CallSiteResult, the *Args set, etc.); the remaining ~10 from Zod schema work in lib/validation/*-schema.ts (S52 WP3) where exported types are not consumed outside the file. exports 52→60 (+8): public surfaces added by S245 WP2A intelligence work (lib/intelligence/workspace-context.ts) plus same Zod-schema work. unlisted 2→3 (+1): @tiptap/core in __tests__/components/item-detail/content-editor.test.tsx. All wired end-to-end; knip flags because consumers reach for concrete query functions / Zod parsers rather than the public type surface. Previously: capturedAt 2026-05-14 (S237 WP6 D1, commit a258d144) — exports 47→52.",
  "counts": {
    "dependencies": 3,
    "devDependencies": 0,
    "exports": 105,
    "types": 71,
    "unlisted": 2,
    "binaries": 0,
    "duplicates": 0,
    "files": 0,
    "enumMembers": 0,
    "unresolved": 0
  }
}
