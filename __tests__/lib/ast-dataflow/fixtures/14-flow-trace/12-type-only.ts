// Fixture: type-only alias — not value-producing
// Used for test 15 (ORIGIN_NOT_VALUE_PRODUCING)
// The type alias at line 4 is the origin under test

export type Foo = {
  id: string;
  name: string;
};
