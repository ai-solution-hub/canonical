# Cloud Run Job manifest — prod / kpf tenant.
# Clone of staging-kpf.yaml with project=kh-prod substituted everywhere.
# See staging-example-client.yaml for full annotations + spec/plan references.
#
# region: europe-west2 (D-RUN-20 deferred — re-evaluate at first-client-data-handling task)
apiVersion: run.googleapis.com/v1
kind: Job
metadata:
  name: kh-pipeline-kpf
  labels:
    tenant: kpf
    env: prod
spec:
  template:
    spec:
      template:
        spec:
          serviceAccountName: kpf-pipeline-sa@kh-prod.iam.gserviceaccount.com
          containers:
            - name: kh-pipeline-slim
              image: europe-west2-docker.pkg.dev/kh-prod-494815/pipeline/kh-pipeline-slim:latest
              env:
                - name: NEXT_PUBLIC_CLIENT_ID
                  value: kpf
                - name: KH_REQUEST_ID
                  value: ""
              resources:
                limits:
                  cpu: "2"
                  memory: 2Gi
              # TODO Phase 2 / T2.2 — Secret Manager mounts (see staging-example-client.yaml).
          maxRetries: 1
          timeoutSeconds: 14400
      parallelism: 1
      taskCount: 1
